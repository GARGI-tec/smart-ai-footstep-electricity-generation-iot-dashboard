{
  "frontend/src/index.css": "Global dark industrial theme with OKLCH color tokens, neon accent CSS variables (amber/green/cyan), custom font imports (Orbitron, Share Tech Mono), glow utility classes, scanline overlay, and flow animation keyframes.",
  "frontend/tailwind.config.js": "Extended Tailwind config with Orbitron/Share Tech Mono/Inter font families, neon color tokens (amber/green/cyan/red), glow box-shadow utilities, and pulse-glow animation keyframes.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for backend interaction — `useLatestReading` polls every 5s, `useSubmitReading` is a mutation, and `useBatteryStatus` returns `null` for batteryLevel (instead of 0) when no reading exists, ensuring no misleading defaults.",
  "frontend/src/App.tsx": "Root app component that renders the DashboardLayout as the single-page application.",
  "frontend/src/components/DashboardHeader.tsx": "Header component with banner background image, project title in Orbitron font with amber glow, system online/AI active status badges.",
  "frontend/src/components/SensorDataPanel.tsx": "Live sensor data card that shows an \"Awaiting Hardware Connection\" placeholder with `WifiOff` icon when no data exists; renders the 2×2 metric grid only when real ESP32 readings are available.",
  "frontend/src/components/AIDecisionPanel.tsx": "Energy analysis card that shows \"AWAITING HARDWARE\" status with muted styling and hides the metrics grid and energy harvested section when no reading exists; shows full analysis with voltage/current/steps metrics only when hardware data is available.",
  "frontend/src/components/EnergyFlowDiagram.tsx": "SVG energy flow diagram with four nodes that renders all arrows inactive (muted grey) when no data exists and overlays a \"No Device Connected\" label; animates active paths based on voltage when real data is present.",
  "frontend/src/components/AnalyticsCharts.tsx": "Sensor analytics panel that shows an \"Awaiting Hardware Data\" placeholder with `WifiOff` icon when no reading exists; renders the four progress-bar metric rows only when real data is present.",
  "frontend/src/components/SimulationControl.tsx": "Status bar showing ESP32 connection state with a `WifiOff` icon and \"NO DEVICE CONNECTED\" label (muted grey) when no reading exists, switching to a pulsing green dot and \"DATA RECEIVED\" when hardware is connected.",
  "frontend/src/components/DashboardLayout.tsx": "Main dashboard layout composing all panels in a responsive grid; now includes HexagonTilePanel, BatteryLevelPanel, and USBOutputStatusPanel positioned below SensorReadingCards and above the existing sensor/AI panels.",
  "frontend/src/components/SensorReadingCards.tsx": "Four-card ESP32 stat grid that shows a centered \"No Device Connected\" placeholder with a `WifiOff` icon when no hardware data exists; only renders the stat cards (Voltage, Current, Energy, Footsteps) when real data is present.",
  "frontend/src/components/BatteryLevelPanel.tsx": "Battery level card that shows a \"No Device Connected\" placeholder with `WifiOff` icon when no data exists; renders the percentage, fill bar, and segmented indicator only when real battery data is available.",
  "frontend/src/components/USBOutputStatusPanel.tsx": "USB output status card that shows an \"Awaiting Hardware\" placeholder with `WifiOff` icon when no data exists; renders the CHARGING/IDLE status dot, label, and output detail grid only when real data is present.",
  "frontend/src/components/HexagonTilePanel.tsx": "SVG hexagon tile visualization panel that shows a centered \"Awaiting Hardware\" placeholder with `WifiOff` icon when no data exists; renders the two animated tiles, connector, and energy summary only when footstep data is available."
}